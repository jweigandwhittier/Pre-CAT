name: pre-cat
channels:
  - conda-forge
  - nodefaults
dependencies:
  # Core requirements
  - python = 3.9
  - pip
  - git

  # For compiling cest-mrf's sim_lin
  - cxx-compiler
  - swig

  # Main application and analysis packages
  - numpy=1.23.5
  - scipy
  - matplotlib
  - pydicom
  - pandas
  - streamlit=1.40.0
  - plotly
  - pillow
  - tqdm
  - pyyaml
  - scikit-learn
  - seaborn
  - sigpy 
  - h5py

  # Pip-installed packages
  - pip:
    - streamlit-drawable-canvas
    - bmctool==0.5.0